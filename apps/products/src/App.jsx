import React, { useState } from "react";

const App = () => {
  return (
    <section>
      <h1>Products</h1>
      <button onClick={() => alert("This button works")} type="button">
        Button in React
      </button>
    </section>
  );
};

export default App;
